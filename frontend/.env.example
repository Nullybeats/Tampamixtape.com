# Backend API URL
VITE_API_URL=http://localhost:3001

# Spotify API Configuration
# Get your credentials at https://developer.spotify.com/dashboard
#
# IMPORTANT: Add these redirect URIs in your Spotify App settings:
#   - http://localhost:5173/callback (local development)
#   - https://tampamixtape.com/callback (production)
#   - https://www.tampamixtape.com/callback (production with www)
#
# The redirect URI is automatically determined based on the current origin,
# so you don't need to set VITE_REDIRECT_URI
VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id_here
